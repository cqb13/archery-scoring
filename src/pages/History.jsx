/*
use firebase to get old scores
get a lot of data, average score, high score, etc...
*/

//TODO: use this to get score data
/*
    const doc = await getDoc(myDoc);
    const encodedData = doc.data.data;
    const data = JSON.parse(encodedData);
  */
